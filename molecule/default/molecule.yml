---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: libvirt
platforms:
  - name: from-public-git
    box: debian/bookworm64
  - name: from-local-clone
    box: debian/bookworm64
provisioner:
  name: ansible
verifier:
  name: testinfra
